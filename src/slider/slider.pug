mixin slider(text)
    .range-filter
        .range
            .flipper.m-left
            .flipper.m-right
            .fill
                .fill-in
        .input-container
            .input-item
                .input-field
                    .label от
                    input.input(type="text")
            .input-item
                .input-field
                    .label до
                    input.input(type="text")