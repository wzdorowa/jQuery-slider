export interface IModelState {
  min: number;
  max: number;
  thumbsValues: number[];
  orientation: string;
  thumbsCount: number;
  step: number;
  isTooltip: boolean;
  isScaleOfValues: boolean;
}
